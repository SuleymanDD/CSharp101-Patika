namespace patika_odev2;

class Koleksiyonlar_Soru_3
{
    /*
        Klavyeden girilen cümle içerisindeki sesli harfleri bir dizi içerisinde saklayan
        Dizinin elemanlarını sıralayan programı yazınız.
    */

    /*
    static void Main(string[] args)
    {
        string inputStr;
        string outputStr="";

        Console.Write("Cumle Girin: ");
        inputStr=Console.ReadLine();
        inputStr=inputStr.ToLower();

        for(int i=0;i<inputStr.Length;i++){
            if(SesliKontrol(inputStr[i])){
                outputStr+=inputStr[i];
            }
        }
        Console.WriteLine(outputStr);
        
    }

    public static bool SesliKontrol(char ch){
        if(ch=='a' || ch=='e' || ch=='ı' || ch=='i' || ch=='o' || ch=='ö' || ch=='u' || ch=='ü'){
            return true;
        }
        return false;
    }
    */
}